# i18n

Supported locales: `ja` (default), `en`. Backend uses Laravel's `__()` / `@lang()`;
the Inertia/React frontend uses [`laravel-react-i18n`](https://github.com/xiCO2k/laravel-react-i18n)
and reads the same `lang/*.json` files via Vite's `import.meta.glob`.

## Translation key convention

Keys are **English source text**: write `__('Cancel')`, not `__('form.cancel')`.
This lets the framework fallback render English directly when no entry is
defined, which we exploit for bundle-size optimisation below.

## Omission policy ("key === text" pruning)

Both `__()` and `laravel-react-i18n` return the key verbatim when a key has no
entry. So entries where `key === value` are redundant.

| File | Pruning rule |
|------|--------------|
| `lang/en.json` | All `key === value` entries removed. English text is the key itself, so the fallback renders correctly. |
| `lang/ja.json` | No optional entries — every referenced key needs a Japanese value. |

`php artisan i18n:check --prune-identity` enforces this once-and-done. The
remaining entry in `lang/en.json` is a Laravel pluralisation form
(`"(and :count more error)|(and :count more errors)|..."`) which is not an
identity match and stays.

When SNS-term placeholder substitution (`%friend%`, `%Communities%`, ...) is
re-added, the policy will extend: pure-placeholder ja keys will also become
optional. That extension lives in `CheckTranslationsCommand` and `useT()`; both
have a single hook point each.

## Locale resolution

[`SetLocale`](../../app/Http/Middleware/SetLocale.php) is registered on
**both** the `web` middleware stack (`bootstrap/app.php`) and the Filament
admin panel stack (`AdminPanelProvider::panel()`) — the Filament panel does
not inherit `web`, so it needs its own entry. It runs in priority order:

1. `session('locale')` — set by `POST /locale` (`route('locale.switch')`)
2. `Accept-Language` header negotiated against `['ja', 'en']`

`Accept-Language` falls back to the first supported locale (`ja`) when there is
no preference. Member-level persistence is not wired yet; add it as step 3 here
once a member-preference store exists.

## Inertia / React wiring

- [`HandleInertiaRequests::share()`](../../app/Http/Middleware/HandleInertiaRequests.php)
  exposes `locale` so the React provider can read it from the initial page props.
- [`app.tsx`](../../resources/js/app.tsx) wraps the Inertia app in
  `LaravelReactI18nProvider` with `fallbackLocale="en"`. Using `en` as the
  fallback (not the app default `ja`) ensures an en miss surfaces as the raw
  English key — otherwise the empty en bundle would silently render Japanese.
- [`useT()`](../../resources/js/lib/i18n.ts) is a thin wrapper around
  `useLaravelReactI18n().t`. Components should import `useT` from `@/lib/i18n`
  rather than the package directly so the SNS-term hook point lands in one
  place when it returns.

## CI gate

`php artisan i18n:check` scans `resources/js/`, `resources/views/`, `app/`,
`database/`, `routes/`, `config/` for `t('...')` / `__('...')` / `@lang('...')`
single-line literals and fails when any key is missing from `lang/ja.json`
(modulo `lang/.i18n-baseline.json` grandfathering — only created on first need
via `--update-baseline`). Dynamic keys (`t($var)`) are silently skipped.

- [`tests/Feature/I18nCoverageTest.php`](../../tests/Feature/I18nCoverageTest.php)
  runs the command as part of `php artisan test` so the gate triggers on every
  test run.
- [`tools/git-hooks/pre-commit`](../../tools/git-hooks/pre-commit) blocks
  commits that touch `*.tsx|*.ts|*.php|*.blade.php` when new gaps appear.
  Install once with `composer install-hooks`.

## Key invariants

1. `lang/en.json` MUST NOT contain `key === value` entries — always run
   `--prune-identity` after `lang:add en` or other bulk imports.
2. Every code-referenced key MUST have a `ja` value (the policy gives `en` a
   free pass, never `ja`).
3. SNS-term placeholders (`%friend%` etc.) are not yet substituted — do not
   introduce them in keys until the SNS-term service lands.
