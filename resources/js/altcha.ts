// Registers the <altcha-widget> custom element. This is the Classic registration form's only JS;
// the Modern form imports the same package. ALTCHA solves the proof of work locally — no external
// calls — so self-hosting it keeps the privacy/offline story intact.
import 'altcha';
