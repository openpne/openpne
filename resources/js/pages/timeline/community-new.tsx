// The community compose page is the same form as the SNS-wide one; it is its own Inertia component
// only so the chrome can crumb back to the community's timeline instead of the SNS-wide feed.
export { default } from './new';
