/**
 * The front page's own component under a second name, so the chrome registry can tell an archived
 * issue from the current one; the page itself is identical by construction rather than by agreement.
 */
export { default } from './issue';
