/**
 * A dated issue, at `/home/{y}/{m}/{d}`.
 *
 * The same screen as the front page, under a second component name so the chrome registry can tell
 * them apart: an archived issue crumbs back to the run of them, and the current one is the hub top
 * with nothing above it. The page itself is identical by construction rather than by agreement.
 */
export { default } from './issue';
