; Sized for the shipped OPENPNE_IMAGE_MAX_UPLOAD_KB default (3 images x 5 MB per post + body,
; docs/internals/images.md "Upload size"); raise these with the cap. The PHP defaults (2M / 8M)
; fail a single phone photo before validation can even name the reason.
upload_max_filesize = 8M
post_max_size = 24M
