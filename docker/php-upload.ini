; Match the app's upload contract (3 images x 5MB per post + body). The PHP defaults
; (2M / 8M) fail a single phone photo before validation can even name the reason.
upload_max_filesize = 8M
post_max_size = 24M
